/**
 * authStore.js — Auth state management (shell).
 * Implementation: React Context + useReducer, or Zustand — TBD in next phase.
 * This file is the single source of truth for session state.
 */

// Placeholder export to satisfy imports during scaffolding.
// Replace with actual store implementation in the auth phase.
export const authStore = {
  session: null,
  user:    null,
}
